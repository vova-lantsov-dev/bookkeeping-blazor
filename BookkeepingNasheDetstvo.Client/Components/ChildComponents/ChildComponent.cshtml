@if (EditChildren)
{
    <ChildInputs Base="child" Child="@Child" ReadStatistic="@ReadGlobalStatistics" />
}
else
{
    <ChildReadOnly Child="@Child" ReadStatistic="@ReadGlobalStatistics" />
}

@functions
{
    [Parameter] private bool EditChildren { get; set; }
    
    [Parameter] private bool ReadGlobalStatistics { get; set; }
    
    [Parameter] private C Child { get; set; }
}
